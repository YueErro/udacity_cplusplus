<?xml version="1.0"?>
<launch>
  <param name="robot_description" command="$(find xacro)/xacro --inorder '$(find my_robot)/urdf/my_robot.xacro'" />
  <!-- Send fake joint values-->
  <!-- Publishes joint state messages for the robot, such as the angles for the non-fixed joints. -->
  <node name="joint_state_publisher" pkg="joint_state_publisher" type="joint_state_publisher">
    <param name="use_gui" value="false"/>
  </node>
  <!-- Send robot states to tf -->
  <!-- Publishes the robot's state to tf (transform tree). The robot_state_publisher publishes the 3D poses of all of the links of the robot. -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" respawn="false" output="screen"/>
</launch>
